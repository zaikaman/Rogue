/**
 * Common constants used across the HTTP module
 */

/**
 * Prefix used to create user IDs for agents in the session service.
 * Format: "user_" + agentPath
 */
export const USER_ID_PREFIX = "user_";

/**
 * Default app name for agent sessions
 */
export const DEFAULT_APP_NAME = "adk-server";
